---
- hosts: all

  vars_files:
    - default.config.yml

  environment:
    PATH: "/opt/homebrew/bin:{{ ansible_env.PATH }}"

  roles:
    - role: brew
    - role: apple_store
    - role: configure_git
    - role: configure_macos
    - role: dock
    - role: zsh
    - role: secrets
    - role: git
    - role: cursor